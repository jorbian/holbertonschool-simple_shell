#include "simple_shell.h"

/**
 * open_repl - opens up the READ-EVALUATE-PRINT-LOOP
 * @shell: double pointer back to teh simple shell itself
 * @repl: double pointer back to UI controls
 * 
 * Return: Exit status of the overal program as executed.
 */
void open_repl(SimpleShell_t **shell, UserInterface_t **repl)
{
    (void)shell;
    (void)repl;
    /* PLACE HOLDER */
}
